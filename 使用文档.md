# Rosmaster Web 遥控系统 — 使用文档

---

## 一、系统要求

### 上位机（小车上的电脑）

| 项 | 要求 |
|----|------|
| 硬件 | 树莓派 4B+ / Jetson Nano 或同等算力 ARM 板 |
| 系统 | Linux（Raspberry Pi OS / Ubuntu 18.04+） |
| Python | 3.7 或以上 |
| 摄像头 | USB 摄像头 或 CSI 深度摄像头，已连接并被系统识别（`/dev/video0`） |
| 串口 | 上位机 USB 口通过串口线连接 MCU 单片机 |

### 遥控端（你的电脑 / 平板 / 手机）

| 项 | 要求 |
|----|------|
| 浏览器 | Chrome / Edge / Firefox / Safari 最新版 |
| 网络 | 与上位机在同一局域网 |
| 键盘 | 物理键盘（PC）/ 触屏按钮（手机） |

---

## 二、部署

### 2.1 上传项目文件

将 `rosmaster_web/` 目录复制到上位机：

```bash
# 方式一: U 盘 / SD 卡拷贝
# 方式二: scp 远程拷贝 (在开发电脑上执行)
scp -r rosmaster_web pi@192.168.1.100:~/Rosmaster-App/
```

整个目录结构需保持完整，因为 `serial_driver.py` 依赖上级目录中的 `py_install_V3.3.1/Rosmaster_Lib/`：

```
Rosmaster-App/
├── py_install_V3.3.1/
│   └── Rosmaster_Lib/
│       └── Rosmaster_Lib.py       ← 串口驱动 (依赖)
├── rosmaster_web/                 ← 本项目 (新)
│   ├── main.py
│   ├── driver/
│   ├── service/
│   ├── web/
│   ├── static/
│   └── ...
```

### 2.2 安装依赖

在上位机上执行：

```bash
cd ~/Rosmaster-App/rosmaster_web
pip install -r requirements.txt
```

需要安装的包：

| 包 | 用途 |
|----|------|
| `pyserial` | 串口通信 |
| `opencv-python` | 摄像头采集 + JPEG 编码 |
| `flask` | Web 服务框架 |
| `flask-sock` | WebSocket 支持 |
| `gevent` | WSGI 服务器（MJPEG + WebSocket 共存） |

### 2.3 确认串口和摄像头

```bash
# 查看串口设备
ls /dev/ttyUSB*   # 通常会看到 /dev/ttyUSB0
ls /dev/ttyAMA*   # 树莓派 GPIO 串口

# 查看摄像头
ls /dev/video*    # 通常会看到 /dev/video0
```

### 2.4 修改配置（按需）

编辑 `config.py`，按实际情况修改：

```python
# 串口 — 改成你的实际设备
SERIAL_PORT = "/dev/ttyUSB0"

# 车型 — 改成你的实际车型
CAR_TYPE = 1   # 1=X3  2=X3 Plus  4=X1  5=R2

# 摄像头 — 0=默认 或设备编号
CAMERA_ID = 0

# 端口 — 默认 8765，一般不需要改
SERVER_PORT = 8765
```

---

## 三、启动

```bash
cd ~/Rosmaster-App/rosmaster_web

# 默认配置启动
python main.py

# 指定串口和车型
python main.py /dev/ttyUSB0 1
```

启动成功后显示：

```
==================================================
Rosmaster Web 遥控系统 启动中...
  串口: /dev/ttyUSB0  车型: 1
  服务地址: http://0.0.0.0:8765
==================================================
初始化串口驱动...
Rosmaster Serial Opened! Baudrate=115200
初始化摄像头...
==================================================
系统就绪!
  浏览器访问: http://<上位机IP>:8765
  键盘: WASD 移动 | Q/E 旋转 | Space 停车
==================================================
```

终端保持运行，`Ctrl + C` 退出。

---

## 四、打开遥控页面

### 4.1 获取上位机 IP

在上位机上执行：

```bash
hostname -I
# 或
ifconfig wlan0 | grep "inet " | awk '{print $2}'
```

例如输出 `192.168.1.100`。

### 4.2 浏览器访问

在同一局域网内，打开浏览器输入：

```
http://192.168.1.100:8765
```

页面加载后即可看到：

```
┌──────────────────────────────────────────┐
│  Rosmaster 遥控面板            ● 已连接   │
├──────────────────────────────────────────┤
│                                          │
│              摄像头实时画面                │
│                                          │
├──────────────────────────────────────────┤
│  🌡 25.5°C    💧 60.2%    🔋 7.8V      │
├────────────────┬─────────────────────────┤
│                │  灯光颜色                │
│  方向按钮       │  🔴🟢🔵⚪🟡🟣✕       │
│  ↺  ▲  ↻      │                         │
│  ◀ STOP ▶     │  灯光特效                │
│     ▼         │  流水 跑马 呼吸 渐变      │
│                │  星光 电量 关灯效         │
├────────────────┴─────────────────────────┤
│  速度: [30%] [60%] [100%]               │
└──────────────────────────────────────────┘
```

---

## 五、操作说明

### 5.1 运动控制

#### 键盘控制（PC 端）

| 按键 | 功能 | 说明 |
|------|------|------|
| **W** 或 **↑** | 前进 | 按住持续前进，松开停止 |
| **S** 或 **↓** | 后退 | 按住持续后退，松开停止 |
| **A** 或 **←** | 左平移 | 按住持续左移，松开停止 |
| **D** 或 **→** | 右平移 | 按住持续右移，松开停止 |
| **Q** | 左旋 | 原地逆时针旋转 |
| **E** | 右旋 | 原地顺时针旋转 |
| **Space** | 紧急停车 | 立即停止所有运动 |
| **1** | 低速 30% | 切换速度档位 |
| **2** | 中速 60% | 切换速度档位（默认） |
| **3** | 高速 100% | 切换速度档位 |

**多键组合**：W+D = 右前方斜向移动，W+A = 左前方斜向移动。

#### 屏幕按钮（手机 / 触屏 / 鼠标）

| 操作 | 效果 |
|------|------|
| 按住方向按钮 | 持续发送运动指令 |
| 松开方向按钮 | 自动停止 |
| 点击 **STOP** | 紧急停车 |

> 手机触屏支持长按。用手指按住方向按钮不放，小车持续运动；手指离开，小车自动停止。

### 5.2 速度档位

页面底部三个速度按钮：

| 档位 | 缩放比例 | 适用场景 |
|------|---------|---------|
| 30% | ×0.3 | 室内狭小空间、精确操控 |
| 60% | ×0.6 | 日常遥控（默认） |
| 100% | ×1.0 | 空旷室外、全速行驶 |

切换档位后立即生效，无需松键重新按。

### 5.3 灯光颜色

点击颜色按钮立即生效，控制**全体灯珠**：

| 按钮 | 颜色 | RGB |
|------|------|-----|
| 🔴 | 红 | (255, 0, 0) |
| 🟢 | 绿 | (0, 255, 0) |
| 🔵 | 蓝 | (0, 0, 255) |
| ⚪ | 白 | (255, 255, 255) |
| 🟡 | 黄 | (255, 255, 0) |
| 🟣 | 紫 | (200, 0, 255) |
| ✕ | 关灯 | (0, 0, 0) |

### 5.4 灯光特效

| 按钮 | effect | 说明 |
|------|--------|------|
| 流水灯 | 1 | 灯光逐颗点亮流动 |
| 跑马灯 | 2 | 灯带两端向中间流动 |
| 呼吸灯 | 3 | 灯光缓慢明暗变化 |
| 渐变灯 | 4 | 颜色渐变过渡 |
| 星光 | 5 | 随机闪烁效果 |
| 电量 | 6 | 灯带显示电池电量 |
| 关灯效 | 0 | 停止特效 |

**颜色和特效互相独立**：先选颜色再开特效，颜色设置会影响呼吸灯/渐变灯等效果。

---

## 六、传感器数据

页面中间的传感器栏实时显示三项数据：

| 数据 | 单位 | 刷新频率 | 说明 |
|------|------|---------|------|
| 温度 | °C | 每 0.5 秒 | 环境温度 |
| 湿度 | % | 每 0.5 秒 | 相对湿度 |
| 电池 | V | 每 0.5 秒 | 电池电压 |

数据由 MCU 自动上报，无需手动刷新。

---

## 七、安全机制

| 机制 | 触发条件 | 效果 |
|------|---------|------|
| **松开即停** | 键盘松键 / 按钮松开 | 立即发送零速度指令 |
| **超时停车** | 超过 0.5 秒未收到新运动指令 | Server 自动发送停车指令 |
| **断线停车** | 浏览器关闭 / WiFi 断开 / 网络超时 | WebSocket 断开触发停车 |
| **页面关闭停车** | 关闭浏览器标签页 | `beforeunload` 事件发送停车指令 |
| **速度边界裁剪** | 发送超限速度值 | Server 端自动裁剪到车型安全范围 |

测试方法：按住 W 前进 → 直接关闭浏览器 → 小车自动停止。

---

## 八、关闭服务

在终端按 `Ctrl + C`：

```
^C
正在关闭...
已停止。
```

程序退出前会自动执行停车指令并释放摄像头资源。

---

## 九、常见问题

### Q1: 启动时报 "No module named 'Rosmaster_Lib'"

确认目录结构正确：

```bash
ls ~/Rosmaster-App/py_install_V3.3.1/Rosmaster_Lib/Rosmaster_Lib.py
# 应该能看到这个文件
```

### Q2: 浏览器打开页面但视频黑屏

检查摄像头连接：

```bash
ls /dev/video*
# 如果看不到 /dev/video0，检查摄像头 USB 线是否插紧

# 测试摄像头
python -c "import cv2; cap=cv2.VideoCapture(0); print(cap.isOpened())"
```

### Q3: 视频延迟大 / 卡顿

WiFi 信号弱或信道拥挤会导致延迟。尝试：
- 将上位机靠近 WiFi 路由器
- 降低视频质量：编辑 `config.py`，将 `JPEG_QUALITY` 从 65 改为 40
- 降低分辨率：将 `FRAME_WIDTH` / `FRAME_HEIGHT` 改为 320×240

### Q4: 页面显示"未连接"

检查：
1. 上位机终端是否仍在运行（无报错）
2. 上位机 IP 是否输入正确
3. 上位机和浏览器是否在同一局域网
4. 上位机防火墙是否放行 8765 端口

### Q5: 按键控制没反应

- 先确认页面连接状态为"已连接"
- 点击一下视频画面区域，确保页面获得了键盘焦点
- 手机端只能使用屏幕按钮，不支持键盘

### Q6: 小车运动方向不对

不同车型的电机接线可能不同。编辑 `config.py` 将 `CAR_TYPE` 改为正确的值：
- X3: `1`
- X3 Plus: `2`
- X1: `4`
- R2: `2`

---

## 十、快速参考卡

```
启动:    cd ~/Rosmaster-App/rosmaster_web && python main.py
访问:    http://<上位机IP>:8765
停车:    Space 键 / 点击 STOP 按钮 / 关闭页面
退出:    Ctrl + C

运动:    W=前进  S=后退  A=左移  D=右移  Q=左旋  E=右旋
速度:    1=30%  2=60%  3=100%
灯光:    点击颜色圆点 → 设颜色    点击特效按钮 → 开灯效
传感器:  温度 / 湿度 / 电池电压 — 自动刷新
```
